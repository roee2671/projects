


public boolean addHole(GeoShape hole) {
if(!contains(outer, hole)){return false;}
for(int i = 0; i < _holes.size(); i++){
    boolean disjoint = disjoint(_holes.get(i),hole);
        if (!disjoint){
        return false;
        }
    }
    return true;
    "123"
    arr.set(i,arr.get(i).subString(0,2));
    "12"
    
}